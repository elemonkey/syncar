name: Deploy SYNCAR

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Crear archivo .env para pruebas
      run: |
        echo "POSTGRES_DB=syncar_db" > .env
        echo "POSTGRES_USER=syncar_admin" >> .env
        echo "POSTGRES_PASSWORD=syncar_password_2024" >> .env
        echo "DATABASE_URL=postgresql://syncar_admin:syncar_password_2024@postgres:5432/syncar_db" >> .env
        echo "REDIS_URL=redis://redis:6379/0" >> .env
        echo "SECRET_KEY=syncar-secret-key" >> .env
        echo "FRONTEND_URL=https://syncar.cl" >> .env
        echo "NODE_ENV=production" >> .env
        echo "ENV=production" >> .env
        echo "NEXT_PUBLIC_API_URL=/api/v1" >> .env

    - name: Install Docker Compose
      run: |
        echo "Instalando docker-compose..."
        sudo curl -L "https://github.com/docker/compose/releases/download/v2.24.6/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
        sudo chmod +x /usr/local/bin/docker-compose
        docker-compose version

    - name: Validate Files
      run: |
        echo "Validating deployment files..."
        
        # Check required files
        files_to_check=("docker-compose.yml" "README.md" "backend/Dockerfile" "frontend/package.json")
        
        for file in "${files_to_check[@]}"; do
          if [ ! -f "$file" ]; then
            echo "Error: $file not found"
            exit 1
          else
            echo "✓ $file exists"
          fi
        done
        
        echo "All required files are present!"
        echo "Repository is ready for deployment"
        
    - name: Test Docker Compose Syntax
      run: |
        echo "Testing docker-compose syntax..."
        docker-compose config --quiet
        echo "✓ docker-compose.yml syntax is valid"
        
    - name: Deployment Summary
      run: |
        echo "=== DEPLOYMENT SUMMARY ==="
        echo "Repository: ${{ github.repository }}"
        echo "Branch: ${{ github.ref_name }}"
        echo "Commit: ${{ github.sha }}"
        echo "Timestamp: $(date)"
        echo "Status: Ready for deployment"
        echo "=========================="